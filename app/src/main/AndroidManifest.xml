<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.helplive.bcm208assignment">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CreateApplication"></activity>
        <activity android:name=".ViewApplicationHO" />
        <activity android:name=".HousingOfficerMenu" />
        <activity android:name=".ViewApplicationApplicant" />
        <activity android:name=".ViewResidenceApplicant" />
        <activity android:name=".ApplicantMenu" />
        <activity android:name=".RegisterHousingOfficer" />
        <activity android:name=".EditResidence" />
        <activity android:name=".SetUpNewResidence" />
        <activity android:name=".AllocateHousing" >

        </activity>
        <activity android:name=".AddResidence" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterApplicant" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>